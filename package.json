{
  "name": "smartdocs",
  "version": "1.0.0-beta",
  "description": "Template-driven, enterprise-style document designer and HTML/PDF renderer.",
  "license": "MIT",
  "type": "commonjs",
  "scripts": {
    "render": "node scripts/render.js",
    "render:pdf": "node scripts/render-pdf.js",
    "demo:video": "node scripts/record-demo-video.js",
    "demo:video:bank": "node scripts/record-demo-bank-integration.js",
    "demo:video:bank:sf": "node scripts/record-demo-bank-salesforce.js",
    "demo:video:bank:sf:desktop": "node scripts/record-demo-bank-salesforce-desktop.js",
    "editor": "node scripts/serve-editor.js",
    "validate": "node scripts/validate.js",
    "check": "npm run validate -- --template examples/template.json && npm run validate -- --template examples/cc-template.json && npm run validate -- --template examples/bank-statement-template.json && npm run validate -- --template examples/terms-template.json && npm run validate -- --template examples/showcase-image-text-template.json && npm run validate -- --template examples/enterprise-program-update-template.json && npm run validate -- --template examples/enterprise-cover-template.json && npm run render -- --template examples/template.json --data examples/data.json --out out/render.html",
    "golden:pdf": "node scripts/golden-pdf.js",
    "golden:pdf:update": "node scripts/golden-pdf.js --update",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f postgres",
    "db:smoke": "node scripts/db-smoke.js"
  },
  "keywords": [
    "document-generation",
    "template-editor",
    "pdf",
    "html-to-pdf",
    "json-template",
    "drag-and-drop"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "ajv": "^8.12.0",
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
